/**
 * @file {{compName}} page
 * @author {{userName}}
 * @date {{today}} 
 */
export default class Store {
    static key = '{{compName}}-page'
    constructor(props) {
        this.rootStore = this;
    }
}